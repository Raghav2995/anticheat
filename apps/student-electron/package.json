{
  "name": "@anticheat/student-electron",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('student tests pending')\""
  },
  "dependencies": {
    "@anticheat/detection-engine": "1.0.0",
    "@anticheat/shared-types": "1.0.0",
    "electron": "^30.5.1"
  }
}
