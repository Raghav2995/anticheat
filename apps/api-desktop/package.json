{
  "name": "@anticheat/api-desktop",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "npm run -w @anticheat/api build && tsc -p tsconfig.json",
    "dev": "npm run build && electron .",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('api-desktop tests pending')\""
  },
  "dependencies": {
    "electron": "^30.5.1"
  }
}
